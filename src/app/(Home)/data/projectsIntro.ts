// Rule 5 fallback copy for the CMS `projectsIntro` section, plus
// `selectedWorksLabel` for both `projectsOneTag`/`projectsTwoTag`. The keys
// predate the CMS and are mapped to the content type's field names at the call
// site (`eyebrow`/`body`/`footnote` → `tagName`/`description`/`footer`).
export const projectsIntro = {
  eyebrow: "WE DESIGN FOR IMPACT",
  body: "We combine behavioural psychology, deep domain expertise and applied AI to build digital products and experiences that move real business numbers - not just look like they should.",
  footnote: "BLR · NYC · DXB",
  selectedWorksLabel: "FEATURED WORK",
};
